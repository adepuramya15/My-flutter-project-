import 'package:flutter/material.dart';

class WorkerRequestsTab extends StatelessWidget {
  const WorkerRequestsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Worker Requests"),
    );
  }
}