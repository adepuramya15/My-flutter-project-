import 'package:flutter/material.dart';

class FindWorkersTab extends StatelessWidget {
  const FindWorkersTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Find Workers"),
    );
  }
}